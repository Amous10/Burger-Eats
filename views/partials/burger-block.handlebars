<li>
	{{name}}

	<button class="devour" data-id="{{id}}" data-newburger="{{uneaten}}">
		{{#if uneaten}}DEVOUR!{{else}}DEVOURED!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
